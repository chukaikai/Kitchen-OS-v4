V54 即時本機保存＋雲端補傳
1. 每一格輸入時立即寫入 localStorage，不等待按儲存。
2. 按儲存會再次本機保存並上傳 Firestore。
3. 雲端失敗保留 pending 標記，網路恢復／重新進頁會自動補傳。
4. Firestore 回傳「不存在」或較舊快照時，不再刪除本機盤點。
5. 頁面切到背景/離開前再次保存本機。
6. 保留 V51 正式央廚清單、V53 防串位規則。
